// import fetch from "jest-fetch-mock";

describe("fetch readings", () => {
    beforeAll(() => {
        jest.clearAllMocks();
        // fetch.enableMocks()
    })
    it("should get an object from request", ()=> {

    })
})